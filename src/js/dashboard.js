export function render(container) {
  container.innerHTML = `
    <h1 class="page-title">Dashboard</h1>
    <p class="empty-placeholder">Statistics, calendar, and objectives land in Phase 4.</p>
  `;
}
